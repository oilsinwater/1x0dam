import React from 'react';
import PropTypes from 'prop-types';

export const PostHeaderText = ({ title }) => {
  return (
    <div>

       </div>
  )
} 

PostHeaderText.PropTypes = { title: PropTypes.string };

PostHeaderText.defaultProps = { title: 'The Publication Title' }